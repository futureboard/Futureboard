{
  "name": "futureboard-extension-template",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "build": "bun run typecheck && cargo build --release",
    "build:debug": "cargo build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
