[package]
name = "xtask"
version = "0.1.0"
edition = "2024"
description = "Futureboard workspace task runner — chains the per-edition cargo aliases"
license.workspace = true
publish = false

[[bin]]
name = "xtask"
path = "src/main.rs"

[lints]
workspace = true
